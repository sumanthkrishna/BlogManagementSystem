<h1>Listing Posts</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th></th>
  </tr>
<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= post.body %></td>
    <td><%= link_to "show", post %></td>
    <td><%= link_to "edit", edit_post_path(post) %></td>
  </tr>
<% end %>
</table>

<%= link_to "New", new_post_path %>

